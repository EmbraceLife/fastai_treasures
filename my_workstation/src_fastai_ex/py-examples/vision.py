from fastai.vision import *
import fastai.vision as fv 
# how many classes and functions are attached to `fv`? see `fv.` tab twice
# how to such attachment made possible? see `fv` source 
path_data = untar_data(URLs.MNIST_TINY)
